# @impetus/mapboxgl

This library was generated with [Nx](https://nx.dev). It is heavily based on [Wykks/ngx-mapbox-gl](https://github.com/Wykks/ngx-mapbox-gl) that at the time of this writting failed to install and compile with Nx.

## Running unit tests

Run `nx test mapboxgl` to execute the unit tests.
